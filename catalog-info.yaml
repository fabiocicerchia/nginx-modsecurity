# Backstage catalog entry, read by paved's scorecard plugin.
#
# Every check paved scores is derived from this file alone — no cluster
# access, no CI credentials. What is absent here is absent from the score.
apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: nginx-modsecurity
  description: >-
      The ModSecurity v3 dynamic module for nginx, as an artifact — a .so you load into your own nginx,
      not another nginx image to adopt.
  annotations:
    github.com/project-slug: fabiocicerchia/nginx-modsecurity
    backstage.io/source-location: url:https://github.com/fabiocicerchia/nginx-modsecurity/
    backstage.io/techdocs-ref: dir:.
  links:
    - url: https://github.com/fabiocicerchia/nginx-modsecurity
      title: Source
spec:
  type: library
  lifecycle: production
  owner: group:default/kodebeat
  system: oss-tools
