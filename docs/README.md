# Documentation

- [Getting Started](getting-started.md) — picking the version, taking the
  artifact, loading it, and the two errors that will happen first.
- [Architecture](architecture.md) — why the final stage is `scratch`, why
  there are two shared objects, and what `--with-compat` actually buys.

The [README](../README.md) is the short version. These pages cover getting it
into your own image without the two hours of guessing.
